"""
sharepoint_ingest.ingestion
───────────────────────────
Internal helpers extracted from the core ingestion engine to keep each
module focused and easy to navigate without a top-tier LLM.

Public API lives in ``sharepoint_ingest.ingestion_engine`` — nothing in
this sub-package should be imported directly by application code.
"""
